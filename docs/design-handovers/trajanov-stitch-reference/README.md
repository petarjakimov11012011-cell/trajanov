# trajanov-stitch-reference — PENDING

This folder is the home for the approved **Google Stitch** export that the Phase 1.02
handover treats as the *visual reference* (the intended look; `brand.md` tokens win
where they differ).

**Status: not yet supplied.** The binary/code assets were not delivered with Phase 1.02,
so nothing has been invented to stand in for them (content-truth rule). The design system
was implemented from `brand.md` + the handover text alone, which is sufficient — the tokens,
not the renders, are authoritative.

## Expected contents (to be dropped in by Lazar/the orchestrator)

```
trajanov-stitch-reference/
├── home/
│   ├── screen.png        # the approved Home render
│   └── code.html         # Stitch's exported markup (reference only)
├── catalog/
│   ├── screen.png
│   └── code.html
├── product_detail/
│   ├── screen.png
│   └── code.html
└── trajanov/
    └── DESIGN.md         # Stitch's design notes
```

When the export is added, delete this note (or trim it to a one-line pointer) and clear the
matching item from the owed-verification register in `src/_project-state/current-state.md`.
